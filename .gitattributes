# Auto-detect text files and normalize to LF in the repository.
* text=auto eol=lf

# Windows batch scripts require CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary release artwork and media are never diffed as text.
*.ico binary
*.png binary
*.iso binary
