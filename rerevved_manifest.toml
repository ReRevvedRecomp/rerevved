[project]
name = "rerevved"
sdk_version = "0.9.0"
game_root = "game"

[entrypoint]
file_path = "game/default.xex"
out_directory_path = "generated/default"
includes = ["rerevved_functions.toml", "rerevved_hooks.toml"]
